DROP TABLE Cause IF EXISTS;

CREATE  TABLE Cause(
id BIGINT GENERATED BY DEFAULT  AS IDENTITY  (START WITH  1, INCREMENT BY 1) NOT  NULL ,
name VARCHAR (100) NOT NULL ,
frequency BIGINT,
total DOUBLE ,
description VARCHAR (100),
PRIMARY  KEY (id)
);